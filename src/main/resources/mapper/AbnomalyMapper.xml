<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.roymark.queue.dao.AbnomalyMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.roymark.queue.entity.Abnomaly">
        <id column="abnomaly_id" property="id" />
        <result column="abnomaly_event" property="event" />
        <result column="abnomaly_start_date" property="startDate" />
        <result column="abnomaly_end_date" property="endDate" />
        <result column="abnomaly_confidence" property="confidence" />
        <result column="abnomaly_link" property="link" />
        <result column="window_id" property="windowId" />
        <result column="user_id" property="userId" />
        <result column="window_name" property="windowName" />
        <result column="user_name" property="userName" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <select id="getAllAbnomaly" resultMap="BaseResultMap">
        SELECT abnomaly.*, window.window_name, user.user_name FROM abnomaly NATURAL JOIN window NATURAL JOIN user
    </select>

    <select id="getByWindowId" parameterType="String" resultMap="BaseResultMap">
        SELECT abnomaly.*, window.window_name, user.user_name FROM abnomaly NATURAL JOIN window NATURAL JOIN user WHERE window_id = #{VALUE }
    </select>

</mapper>
